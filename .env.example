PUBLIC_SANITY_PROJECT_ID=
PUBLIC_SANITY_DATASET=production

# Set to "true" only on the Production environment in your host's project
# settings (not here) to show the "Back soon" page site-wide instead of
# real content. Leave unset on Preview deployments so those keep showing
# the full site.
SITE_LOCKED=
